# The shamefully-hoist option tells pnpm to hoist all packages to the root node_modules directory.
# It replicates npm’s flat dependency layout to improve compatibility with packages that expect it.
# https://pnpm.io/npmrc#shamefully-hoist
shamefully-hoist=true
