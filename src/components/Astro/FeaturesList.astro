---
/* Component for rendering a list of features with titles and optional descriptions.
 *
 * Props:
 * - features: Array<{
 *     title: string,
 *     description?: string
 *   }> - Array of feature objects
 * - class?: string - Additional CSS classes
 *
 * Example usage:
 * <FeaturesList
 *   features={[
 *     {
 *       title: "Responsive Design",
 *       description: "Looks great on all devices"
 *     },
 *     {
 *       title: "Dark Mode",
 *       description: "Automatic theme switching"
 *     }
 *   ]}
 *   class="my-custom-list"
 * />
 *
 * Notes:
 * - Animated highlight effect on hover
 * - Monospace font for feature titles
 * - Indented descriptions with italic styling
 * - Smooth transitions for visual effects
 * - Theme-based accent colors
 */

interface Props {
	features: {
		title: string;
		description?: string;
	}[];
	class?: string | undefined;
}

const { features, class: className } = Astro.props;
---

<ul class:list={[className]}>
	{
		features.map((feature) => (
			<li class="relative mb-8">
				<h3 class="text-xl font-mono font-semibold text-theme-accent build-in mb-0">
					{feature.title}
				</h3>
				{feature.description && (
					<p class="ml-4 italic text-theme-primary fade-in mt-2 text-lg">
						{feature.description}
					</p>
				)}
			</li>
		))
	}
</ul>

<style>
.fade-in {
animation-delay: 0.25s;
}
</style>
